# Backend server port
PORT=3001

# SQLite DB file location (relative to backend folder)
DB_PATH=./data/calendly.db

# SMTP email config (optional)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_FROM="noreply@example.com"
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
